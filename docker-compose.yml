version: "3"
services:
  app:
    build: .
    ports:
      - "3001:3000"
    environment:
      - NODE_ENV=development
      - PORT=3000
    volumes:
      - todo-logs:/data
volumes:
  todo-logs:
    external: true
